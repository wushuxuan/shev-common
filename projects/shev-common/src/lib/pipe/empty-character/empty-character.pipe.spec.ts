import { EmptyCharacterPipe } from './empty-character.pipe';

describe('EmptyCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new EmptyCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
